[workspace]
resolver = "3"
members = ["crates/*"]

[workspace.package]
version = "1.4.1"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
repository = "https://github.com/leandrodaf/3d-new-era-ai"
homepage = "https://github.com/leandrodaf/3d-new-era-ai"
authors = ["Leandro Ferreira"]
keywords = ["3d", "home-design", "floor-plan", "mcp", "ai"]
categories = ["graphics", "command-line-utilities"]

[workspace.dependencies]
# Internal crates
newera-core = { path = "crates/newera-core", version = "1.0.0" }
newera-mcp = { path = "crates/newera-mcp", version = "1.0.0" }
newera-telemetry = { path = "crates/newera-telemetry", version = "1.0.0" }
newera-server = { path = "crates/newera-server", version = "1.0.0" }
newera-app = { path = "crates/newera-app", version = "1.0.0" }
newera-draw = { path = "crates/newera-draw", version = "1.0.0" }
newera-catalog = { path = "crates/newera-catalog", version = "1.0.0" }
newera-sh3d = { path = "crates/newera-sh3d", version = "1.0.0" }
newera-render = { path = "crates/newera-render", version = "1.0.0" }
newera-plugins = { path = "crates/newera-plugins", version = "1.0.0" }
newera-joinery = { path = "crates/newera-joinery", version = "1.0.0" }
newera-ergonomics = { path = "crates/newera-ergonomics", version = "1.0.0" }

# External crates
ab_glyph = "0.2.32"
anyhow = "1.0.104"
base64 = "0.23.1"
axum = "0.8.9"
bytemuck = { version = "1.25.2", features = ["derive"] }
eframe = "0.36.2"
epaint_default_fonts = "0.36.2"
image = { version = "0.25.10", default-features = false, features = ["png", "jpeg", "webp", "bmp"] }
futures-util = { version = "0.3.31", default-features = false, features = ["std"] }
glam = { version = "0.33.7", features = ["bytemuck"] }
clap = { version = "4.6.6", features = ["derive", "env"] }
rmcp = { version = "3.3.0" }
schemars = "1.2.2"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"
tiny-skia = "0.12.0"
tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread", "signal", "sync"] }
tokio-util = "0.7"
tower-http = { version = "0.7.1", features = ["cors", "trace"] }
tracing = "0.1.44"
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
tracing-subscriber = { version = "0.3.23", features = ["env-filter", "fmt"] }

[workspace.lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "warn"
unreachable_pub = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
missing_errors_doc = "allow"
# Geometry code reads best with math-style names (p, q, a, b, n).
many_single_char_names = "allow"
similar_names = "allow"
# UI and tool dispatch read top to bottom; splitting them hurts more than it helps.
too_many_lines = "allow"
# The home model mirrors real settings (visibility flags, locks) and
# element variants differ in size by nature; boxing them buys nothing.
struct_excessive_bools = "allow"
large_enum_variant = "allow"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true

# Keep debug builds lean: full debug info only for our crates, and optimized
# dependencies so the 3D view and geometry stay fluid while developing.
[profile.dev]
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false
opt-level = 2
