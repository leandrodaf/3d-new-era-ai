# Auditoria do fluxo de projeto por IA

Registro vivo iniciado em 20/09/2026 durante a revisão da planta **Clássico do Upper West Side**. Pedido do usuário: registrar todo trabalho manual, cálculo, diagnóstico ou checagem que o New Era poderia realizar automaticamente. Atualizar este arquivo conforme a revisão avança; não tratar propostas como funcionalidades já implementadas.

## Contexto e evidências

- Planta inicial conectada: revisão 1, 13 paredes, 10 ambientes e 135 peças, sem cotas nem etiquetas.
- `check_layout({})` inicialmente retornou somente uma sobreposição aceita entre escrivaninha e cadeira (`f90` / `f91`). Isso não significava um projeto funcional: `ergonomics` encontrou móveis virados para paredes, circulação inadequada e ausência de janela na suíte.
- A planta inicial coincide em nome, contagens e IDs com `web/demo.newera`. Uma cópia local foi aberta em uma instância separada do New Era para inspeção visual enquanto a renderização da aba online estava quebrada.
- Evidências locais desta execução: `target/plan-review/` (imagens, respostas de ferramentas e auditorias), `/tmp/newera-fix-check.log` e `/tmp/newera-wasm-clippy.log`. Esses caminhos são temporários e não substituem os testes e casos reproduzíveis descritos abaixo.
- Backup da revisão de layout na cópia local: `target/plan-review/upper-west-side-layout-revisado.newera`. Na aba, checkpoint **Layout corrigido — quartos, cozinha e orientação**, revisão 5. O arquivo preserva as alterações geométricas; não representa todo o histórico/checkpoints da aba.

### Validação da correção do crash

- **Antes:** o teste real no Chrome reproduziu `no filesystem on this platform` e `Uncaught RuntimeError: unreachable` ao executar `render_plan`.
- **Depois:** `make web-editor` passou; `scripts/web-mcp-e2e.mjs` passou com WebGPU e novamente com `NO_WEBGPU=1` (WebGL), incluindo as três renderizações, edição posterior, reconexão e fechamento efetivo da aba.
- `cargo clippy -p newera-mcp --target wasm32-unknown-unknown --locked -- -D warnings` passou.
- `make check` passou: formatação, Clippy de todo o workspace, 462 testes aprovados e smoke test MCP. A suíte marcou 29 testes opcionais como ignorados; não foram considerados provas de validação visual.
- O [Publish 35508133780](https://github.com/leandrodaf/3d-new-era-ai/actions/runs/35508133780) bloqueou o deploy: o teste adicional encontrou uma segunda causa de panic no WebGPU do runner Linux, descrita abaixo. A correção local de filesystem não deve ser confundida com publicação concluída.

## Falhas do software e oportunidades de automação

| Caso | Evidência / trabalho manual | Comportamento desejado | Verificação de aceite |
| --- | --- | --- | --- |
| **P0 — Renderizar a planta aborta o editor web** | `render_plan` chama `scene_options_for`, que cria `TopViews` usando `cache_dir()`. Sem variáveis de ambiente no WASM, chega a `std::env::temp_dir()`, que aborta com `no filesystem on this platform`; no navegador aparece `Uncaught RuntimeError: unreachable`. Foi necessário reproduzir via Chrome/CDP e ler o panic no console. | O caminho web deve usar os mesmos símbolos da planta na tela, sem acessar cache de disco. Auditar dependências de filesystem, threads e outras APIs nativas em todas as ferramentas expostas no navegador. | Imagem 2D, imagem 3D, outra imagem 2D e uma edição devem funcionar em sequência pelo relay, sem panic nem tela fatal. Teste em `scripts/web-mcp-e2e.mjs`; correção em `crates/newera-mcp/src/tools/render.rs`. |
| **P0 — Publicação não testava renderização MCP** | O teste anterior desenhava uma parede, mas não solicitava imagens. O fluxo Publish testava interação visual, enquanto CI rodava separadamente. Uma publicação podia passar sem exercitar a operação que derrubava a aba. | Testar o fluxo real da IA antes de publicar: ler, editar, renderizar, continuar editando, reconectar e encerrar. | Publish deve depender do sucesso do teste MCP; falhas de imagem devem impedir o passo de deploy. Alteração preparada nesta revisão. |
| **P0 — WebGPU aborta ao mapear buffer de geometria** | No Publish 35508133780, o Chrome recusou `createBuffer` de 2.860.312 bytes com `mappedAtCreation == true`; `wgpu` fez `unwrap()` do erro e abortou o WASM após a edição via MCP. Passou no Mac local, mas falhou no Linux do runner. A IA precisou inspecionar o log do GitHub e rastrear `SceneGpu::upload_mesh` (em `view/scene.rs`). | Evitar o mapeamento inicial da cena no navegador. Criar buffers sem mapeamento e transferir por `queue.write_buffer` em blocos limitados; evitar cópia temporária concatenando índices opacos e transparentes. | E2E injeta a recusa de buffers de cena mapeados e exige edição/renderização posteriores sem fatal. Rodar WebGPU e WebGL e confirmar Publish no Linux. **Testado localmente em WebGPU e WebGL; commit `86e747a` enviado; [Publish 35508995352](https://github.com/leandrodaf/3d-new-era-ai/actions/runs/35508995352) passou, incluindo o teste MCP no Linux e deploy pelo GitHub.** |
| **P1 — Diagnóstico fatal sem contexto** | O HTML informa “não conseguiu iniciar” mesmo quando o editor já iniciou e falhou durante uma ferramenta. A resposta MCP ficou pendente e depois informou que a aba saiu, sem explicar o panic original. | Diferenciar falha de inicialização de falha durante uso. Registrar ferramenta, fase, revisão, build, backend gráfico e causa original, sem incluir segredos. Permitir que a IA obtenha diagnóstico estruturado. | Uma falha injetada durante renderização deve identificar a operação e a causa; não deve culpar genericamente o navegador. **Ainda não implementado.** |
| **P1 — Persistência e recuperação da sessão** | A fonte autoritativa é a aba; após o crash a conexão se perdeu. Foi necessário usar a cópia local para inspeção e aguardar o retorno da conexão. Foi criado checkpoint antes das edições, mas checkpoint sozinho não prova recuperação após perda da aba. | Autosave durável no navegador, recuperação após panic/reload e ferramenta para verificar status e revisão salvos. Exportação/download pelo MCP deve ter semântica própria no navegador. | Após editar e interromper a aba, reabrir deve recuperar a última revisão confirmada, com backup exportável. **Ainda não verificado/implementado nesta revisão.** |
| **P1 — “Sem colisões” não significa “pronto”** | A checagem de layout inicial não trouxe os móveis inutilizáveis que apareceram em ergonomia. A IA precisou combinar `get_home`, `check_layout`, `ergonomics`, imagens, orientação e medidas. | Auditoria unificada, com módulos e escopo explícitos: geometria, uso, aberturas, instalações, iluminação e apresentação. Resposta deve declarar o que não foi verificado. | A planta inicial deve destacar armários/louças voltados para paredes e quarto sem janela, mesmo sem sobreposição geométrica. |
| **P1 — Correção isolada pode piorar outra restrição** | Girar `f92` para dentro do quarto resolve a orientação, mas revela somente 46 cm diante das portas. Girar `f155` revela 40 cm diante da ilha. Girar `f79` deixa 8,5 cm diante de parte da cama. Foi preciso testar conjuntos de alterações. | Resolver relações entre móveis em conjunto, com propostas reversíveis e comparação antes/depois. Não apresentar uma rotação como solução completa quando a peça continua inutilizável. | Uma proposta deve avaliar circulação, abertura e colisões de todas as peças afetadas; recusar ou explicar regressões antes de aplicar. O dry-run atual ajuda, mas a composição ainda foi manual. |
| **P1 — Semântica do ambiente incoerente** | `r18` se chamava “Copa”, mas continha vaso e box. O sistema recomendava fogão, geladeira e zonas de cozinha. Além disso, `f96` era uma pia de cozinha nesse banheiro. | Detectar conflito entre nome, tipo do ambiente e equipamentos. Sugerir correção sem renomear silenciosamente nem trocar o programa do usuário. Permitir tipo explícito independente do nome. | O conjunto vaso + chuveiro deve disparar revisão da classificação “copa”; o lavatório precisa ser realmente do tipo correto, não apenas receber novo nome. |
| **P1 — Aberturas e móveis altos** | A suíte não tinha janela. No closet, um armário alto estava diante da janela. O armário aéreo da cozinha também precisava ser retirado da faixa de janela. A checagem inicial não destacou esses casos. | Checar o volume real das aberturas contra marcenaria, incluindo folha, ventilação e acesso; distinguir móvel baixo de bloqueio alto. | Testes com armário cruzando janela devem produzir achado com IDs, altura e trecho bloqueado. Uma janela parcialmente bloqueada não deve contar integralmente como abertura útil. |
| **P2 — Medidas ainda precisam ser calculadas pela IA** | Foi necessário calcular caixas orientadas, folgas entre ilha e bancada, posição da cabeceira, área ocupada pelas portas e comprimento de armários. Exemplos: bancada lateral com face em x=72,5; ilha começando em x=167,5; corredor resultante de 95 cm. | Oferecer operações relacionais: encostar costas à parede, manter corredor mínimo, alinhar criados à cabeceira, posicionar assentos voltados para mesa/TV. Recalcular dependências ao redimensionar. | Proposta deve fornecer restrições satisfeitas e medidas verificáveis, sem exigir que a IA calcule centros a partir de faces. |
| **P2 — Cabeceira e mobiliário de apoio** | As camas estavam a 180°, mas os criados estavam próximos ao pé. A IA precisou interpretar o modelo e mover cama, criados e abajures juntos. | Relações semânticas entre cama, cabeceira, criados, luminárias e tapete; detectar criados no extremo errado. | Mover/reorientar a cama deve oferecer acompanhamento das peças associadas, sem separar abajur de apoio nem bloquear a circulação lateral. |
| **P2 — Resultados numéricos confusos nas aberturas** | O dry-run de janelas mostrou folgas negativas enormes contra a própria parede hospedeira, por exemplo `f25` com -342,5 e -1482,5 cm em direções ao longo de `w1`. | Medidas de abertura devem considerar vão, ombreiras e anfitrião, sem apresentar a parede onde ela está embutida como obstáculo comum. | Janela válida hospedada deve retornar largura de vão e distâncias aos cantos/aberturas vizinhas; sem falsa interpenetração com o anfitrião. |
| **P2 — Renomear agrupa achados como novos/resolvidos** | Ao renomear Copa para Banho social, o dry-run listou diversos pontos hidráulicos/elétricos ausentes como resolvidos e novos, embora continuassem ausentes. Isso dilui o efeito real da mudança. | Identidade estável por elemento/regra, com alteração de descrição separada de resolução. | Renomear um ambiente não deve contar a mesma falta de água/esgoto como defeito resolvido. |
| **P2 — Pontuação mistura estágios de projeto** | A pontuação inicial de ergonomia foi 29. Grande parte dos achados eram falta de circuitos, tomadas, hidráulica e telecom em uma planta de arquitetura/interiores. Foi necessário separar manualmente problemas de uso e disciplinas ainda não projetadas. | Declarar escopo e maturidade do projeto. Separar “ausente no desenho desta disciplina” de “defeito do layout”. Manter visível o trabalho pendente sem apresentar score único como certificado de qualidade. | Auditoria arquitetônica deve ser completa no seu escopo e declarar instalações não verificadas; auditoria executiva deve exigir as disciplinas solicitadas. |
| **P2 — Aceitação ficou órfã depois da correção** | Após reposicionar escrivaninha e cadeira, a aceitação `overlap:f90+f91` permaneceu órfã. `check_layout` a informou, mas foi necessário interpretar e limpar separadamente. | Propor limpeza de aceitações que deixaram de corresponder a um achado, junto da edição que resolveu o problema. Preservar histórico para auditoria. | Aceitação antiga não deve esconder um conflito novo nem poluir o relatório atual; remoção deve ser reversível. |
| **P2 — Teste confundia navegação com fechamento** | Mesmo após as renderizações passarem, a checagem final do E2E demorava: navegar para `about:blank` não garante destruição do documento/socket, por cache de navegação. | No teste de fechamento, fechar realmente o target do Chrome; no teste de navegação, especificar separadamente o comportamento esperado. | `Target.closeTarget` seguido de chamada MCP deve informar desconexão. Correção incluída no teste. |
| **P2 — Critério visual ausente** | Um relatório de colisões limpo não avalia composição, detalhe da marcenaria, coerência de materiais, iluminação, legibilidade de nomes/cotas ou enquadramento. A IA precisou renderizar e revisar por conta própria. | Oferecer vistas de revisão predefinidas por ambiente, cortes úteis e checklist de apresentação separado das regras funcionais. | Entrega de interiores deve incluir revisão visual do conjunto e dos ambientes, além de auditoria geométrica; sem confundir imagem atraente com projeto construtivo validado. |
| **P1 — Pendentes ultrapassam o teto sem aviso** | O corte revelou `f54`/`f58` com base a 250 cm e altura de 90 cm: topo a 340 cm para teto de 300 cm. `f70`/`f71` chegavam a 320 cm. A IA calculou `elev + h` e corrigiu as bases para 210 cm; `f102` ficou com base 230 e altura 70 cm. `check_layout` não acusava esses excessos. | Validar envelopes verticais contra teto/forro e atribuir altura de suspensão relativa ao teto. Diferenciar cabo de corpo luminoso. | Pendentes excedendo o teto devem ser apontados com excesso em cm; alterar o pé-direito deve atualizar ou invalidar suas ancoragens. |
| **P1 — Cor do catálogo é aplicada duas vezes** | Após recolorir a cozinha de verde, o renderizador substituiu também inox, tampo e ferragens por verde, tornando a cuba visualmente indistinguível. `piece_mesh` já usa a cor no corpo da peça, mas `Mesh::add_piece` sobrescrevia todas as cores de vértice novamente. | Modelos procedurais devem preservar os materiais secundários definidos pelo catálogo. Modelos importados devem continuar aceitando substituição global de cor. | Teste de bancada recolorida deve preservar corpo verde e materiais distintos da cuba, tampo e metais; teste separado garante a substituição global em modelos importados. 27 testes do renderizador passaram, incluindo preservação dos materiais secundários e override dos importados; commit `86e747a` enviado. |
| **P1 — Marcenaria na face errada de parede interna** | `joinery(kind=slats,wall=w8,along=150,angle=270)` criou `f199` em x=872,35, no jantar, embora a frente apontasse para o estar. O código escolhe o lado pelo centro global da casa e aplica o ângulo explícito depois de deslocar a peça. A IA precisou mover o centro para x=887,65: face da parede 886 + metade da profundidade 1,65. | Lado/ambiente escolhido explicitamente deve prevalecer sobre heurística global. Determinar orientação e face antes de calcular a posição; mostrar ambiente resultante no retorno. | Reproduzir parede interna vertical com ambientes dos dois lados: uma peça apontando para +x deve ficar na face leste, com as costas encostadas e sem interpenetração. **Correção na planta aplicada; código ainda pendente.** |
| **P2 — LED de sanca é apenas geometria** | A ferramenta `joinery` com `led=true` criou peças `box` amarelas chamadas “Fita de LED”, sem saída luminosa em `get_home(parts=true)`. A IA precisou inspecionar peças e implementação para perceber que a especificação visual não constitui uma luminária calculada. | Gerar emissores com comprimento, potência/fluxo e direção coerentes com a sanca; identificar explicitamente quando um elemento é só representação. | Acrescentar fita deve alterar a simulação luminosa com parâmetros rastreáveis. Não contabilizar apenas uma caixa amarela como circuito ou fonte de luz validada. |

## Registro das intervenções na planta

### Aplicadas e verificadas por geometria

Antes de editar, criado checkpoint **Antes da revisão de layout e acabamento**. As operações são comandos reversíveis do próprio New Era.

1. **Orientação e circulação — revisão online 3, 23 peças.**
   - Armários `f81`, `f92`, `f95`, lavatório `f84`, vasos `f85`/`f97`, sofá `f45`, poltronas `f46`/`f47` e conjunto TV `f156`/`f157` voltados para uso no interior.
   - Suíte: cama `f72` em [1100,975]; criados/abajures em y=1060; cômoda `f79` em [1230,705]; poltrona `f78` em [955,800].
   - Quarto 2: cama `f87` em [650,975]; criados em y=1060; escrivaninha `f90` em [510,785], cadeira `f91` em [605,785] e armário `f92` em [840,800].
   - O dry-run resolveu 14 apontamentos, sem novos achados; score 29 → 39. `check_layout` após aplicação não trouxe conflitos, somente a aceitação órfã da cadeira/escrivaninha. A pontuação não comprova acabamento nem projeto executivo completo.

2. **Cozinha, jantar e banheiro social — revisão online 4, 12 elementos.**
   - Bancada lateral `f155`: [42,5;260], 160 cm, frente para +x; ilha `f66`: [270,265], 205 × 100 cm. Corredor entre faces: 95 cm. Banquetas reposicionadas.
   - Geladeira `f64` em [412,125], voltada para -x, mais próxima da área de cocção; armário aéreo `f69` transferido à parede lateral.
   - Mesa de jantar `f55` deslocada 5 cm; mesa de centro `f48` deslocada 10 cm para liberar o sofá.
   - Bancada `f96` reduzida/reposicionada e vaso `f97` em [65,835], deixando 85 cm entre os volumes; box `f98` voltado para a entrada do ambiente.
   - “Copa” renomeada **Banho social**. Falta substituir o catálogo da pia de cozinha por lavatório: a troca de nome não resolve isso.
   - `check_layout` após aplicação novamente sem conflitos, somente aceitação órfã. Imagem 2D de verificação gerada na cópia local com as mesmas alterações.

3. **Aberturas e referência vertical — revisões online 6 a 10.**
   - Nível `lv163` corrigido para 300 cm.
   - Armário `f81` transferido para a parede sul do closet, com frente para norte e 84 cm entre frentes. Janela `f31` ajustada à faixa desobstruída entre armários.
   - Janela da suíte `f165` acrescentada em `w3`, 180 × 140 cm, peitoril a 140 cm; basculante `f166` no banho social, 150 × 80 cm, peitoril a 170 cm. Aberturas existentes de cozinha, jantar, quarto 2 e banho da suíte redimensionadas.
   - Painel `f86` reduzido para 90 cm e afastado da parede exterior, sem atravessá-la.
   - Pia de cozinha `f96` substituída pelo lavatório real `f167`, 120 × 48 × 85 cm; aceitação órfã removida. `check_layout` retornou `{}`.

4. **Forros, painel de TV, alturas e materiais — até a revisão online 20.**
   - Sancas paramétricas de 18 cm no estar (`f168`) e jantar (`f181`), com 12 componentes cada; tabica de 2 cm na galeria (`f194`), quatro componentes.
   - Painel de nogueira `f199`, 220 × 260 cm, 44 ripas de 35 × 18 mm sobre fundo, com espaçamento ajustado pelo gerador. Corrigida manualmente a face de instalação; rack e TV aproximados do painel.
   - Pendentes ajustados para terminar no teto; paleta de madeira, linho, verde e tons de areia aplicada. A revisão visual expôs o defeito de recoloração registrado acima; acabamento ainda não aprovado como final.
   - Planta atual: 141 peças principais, 10 ambientes, 13 paredes e pé-direito 300 cm. `check_layout({})` voltou a retornar `{}`; isso não substitui a revisão visual nem as disciplinas pendentes.

### Identificadas, ainda pendentes

- Revisar o acesso e detalhamento completo do banho da suíte; o painel foi corrigido, mas o conjunto ainda precisa de revisão de acabamento.
- Acabamento detalhado, iluminação e apresentação ainda em revisão. Não declarar a planta concluída com base apenas nas correções acima.
- Instalações não desenhadas: esclarecer escopo e não esconder ausências por aceitações genéricas.

## Como continuar este registro

Para cada novo caso, registrar: operação que o revelou; IDs/revisão; evidência; interpretação; cálculo ou trabalho adicional feito pela IA; intervenção; verificação; automação proposta e teste de aceite. Marcar claramente **observado**, **hipótese**, **corrigido e testado** ou **pendente**. Atualizar o estado após o teste e após o deploy; código local não equivale a correção online.

## Portas, janelas e eletrodomésticos — revisão de 20/09/2026

**Pedido explícito:** impedir móveis/eletrodomésticos obstruindo portas e móveis altos/coifas diante de janelas, sem tratar uma airfryer sobre bancada como erro só porque há janela atrás.

- **Observado:** a ampliação da janela `f25` deixou 30 cm da coifa `f63` sobrepostos à sua faixa horizontal. O relatório anterior retornava `{}` porque não examinava o volume diante das janelas. Na revisão online **21**, a janela passou de x=240 para x=210; cooktop, forno e coifa (`f61`–`f63`) de x=330 para x=340. Janela termina em x=300; coifa começa em x=310: folga lateral de 10 cm. O dry-run desse conjunto não criou achados. Alternativas descartadas bloqueavam o forno ou a pia, ou alongavam o triângulo de trabalho.
- **Automação implementada e testada:** `blocks_window` cruza a faixa da abertura com altura e projeção dos objetos próximos (até 60 cm de cada face), respeitando rotação, visibilidade e pavimento. Retorna IDs e extensão obstruída `[largura, altura]` em cm. Inclui aparelhos embutidos: o encaixe no móvel não autoriza obstruir uma janela.
- **Exceção geométrica explícita:** objeto compacto de bancada, até 60 × 60 × 50 cm, base até 110 cm e topo no máximo 40 cm acima do peitoril, não gera esse achado. Não depende do nome “airfryer”. Objetos abaixo do peitoril ou acima da abertura também não bloqueiam seu volume. Isso é uma heurística inicial para bloqueio volumétrico, **não** valida abertura da folha, acesso ao puxador, ventilação do aparelho ou distâncias exigidas pelo fabricante; esses casos continuam como melhoria pendente.
- **Falha adicional:** `blocks_door` só examinava o giro de portas convencionais. Agora também verifica aproximação de 60 cm dos dois lados de portas de correr e passagens, conservando a verificação da folha e as exceções de tapetes/objetos acima do vão. A função usada para sugerir correções de porta usa a mesma regra.
- **Testes de regressão:** coifa parcialmente sobreposta, geladeira, airfryer elevada sobre bancada, objeto lateral/distante/acima, rotações de 0°, 90° e 37°, objeto invisível, aparelho embutido, porta de correr/passagem bloqueada dos dois lados, tapete e objeto acima da porta. A primeira execução encontrou erro no cálculo angular (graus/radianos); corrigido usando a transformação nativa `to_local`, antes de publicar.
- **Limites a automatizar depois:** curso real de folhas de janelas e ferragens; classificação semântica de aparelhos e parâmetros de instalação; circulação completa além da faixa local de 60 cm; avaliação por triângulos/volume de modelos importados quando sua caixa é conservadora.

- **Aplicação da regra à planta:** encontrados aparador `f56` no arco do jantar, aparador `f99` diante da porta da cozinha e estante `f52` na faixa da janela `f30`. Ajuste validado em dry-run sem novos achados: `f56` em [670;27,5], profundidade 30 cm, frente +y (52,5 cm até o conjunto da mesa); `f99` em [370;510]; `f52` em [1460;430], largura 160 cm. Aplicado e salvo na cópia local versionada em [`projects/upper-west-side-revisao.newera`](projects/upper-west-side-revisao.newera). **Após HTTP 524 e “the editor’s tab went away”, o usuário restabeleceu a conexão. A leitura confirmou que as três peças ainda estavam nas posições antigas; a atualização foi reaplicada e confirmada na nova sessão (revisão 2). Nova leitura confirmou também janela/coifa nas posições corrigidas.** A coifa/janela teve confirmação online na revisão 21.
- **Falsos positivos identificados na revisão:** caixas envolventes de luminária de piso, planta em vaso e vidro do box não são sólidos opacos equivalentes a marcenaria. A regra foi refinada para excluir luminárias com parâmetros de luz, catálogo `plant`/`shower-glass` e opacidade menor que 0,5. Não ocultar bloqueios físicos dessas peças nas regras de passagem de portas.
- **Nova oportunidade de automação:** timeout HTTP 524 após uma mutação não informa se ela foi aplicada. É necessário ID idempotente de operação, consulta de resultado/revisão e timeout estruturado; até lá, ler o estado antes de repetir. O backup local preserva o trabalho, mas não comprova que a aba recebeu a última edição.

- **Verificação integrada:** servidor nativo recompilado com a regra nova abriu o backup versionado; `check_layout` retornou `{}` após os ajustes. Checkpoint online criado: “Portas e janelas desobstruídas — revisão de layout”.

- **Renderização na aba real após reconexão:** `render_plan` 1000 × 700 retornou PNG normalmente e a aba continuou respondendo ao MCP. A correção de crash publicada foi exercitada no projeto do usuário, além do E2E.

- **Regressão de ergonomia:** o teste de inversão de dobradiça esperava somente o móvel atingido pela folha. Com a verificação de aproximação, ambos os móveis a 60 cm do vão bloqueiam a porta independentemente da dobradiça. A expectativa foi atualizada para os dois IDs, preservando a exigência de não sugerir uma inversão que deixe a passagem bloqueada.
- **Prova de detecção no projeto:** dry-run restaurando a janela/coifa às posições problemáticas gerou `blocks_window` com extensão `[30,60]` cm; a planta corrigida retornou `{}` no servidor com a regra nova. O dry-run não alterou o arquivo nem a aba.

- **Contrato MCP:** o teste `tool_surface_is_unchanged` fixa o tamanho serializado de todas as ferramentas. A documentação das regras novas aumentou o total de 85.924 para 86.226 bytes, exigindo atualização explícita da expectativa. O conjunto de nomes/schema foi preservado. Melhoria sugerida: snapshot legível por ferramenta, com diferença de campos/descrições, em lugar de apenas contagem agregada de bytes.

- **Validação do código:** formatação e Clippy do workspace aprovados; 466 testes passaram, zero falhas, 29 testes opcionais explicitamente ignorados. Inclui a exceção de aparelho compacto sobre bancada, obstrução por coifa/geladeira, rotação, transparência e acesso às portas/passagens.

- **Gate local concluído:** `make check` terminou com código 0, incluindo smoke MCP com edição, renderizações 2D/3D, salvar e reabrir projeto. Publicação da regra nova fica a cargo do workflow GitHub após o push.

## Publicação web e VPS — esclarecimento operacional

- Os commits de renderização/layout alteram o editor WASM servido pela Cloudflare; o relay da VPS não depende de `newera-core`, `newera-mcp` ou `newera-render`. Nenhuma alteração no binário do relay exigiu deploy de VPS nesta revisão.
- Há workflow `deploy-vps.yml`: tag semver → Clippy/testes do relay em runner GitHub → imagem GHCR → atualização por SSH/túnel → `/health`. Último deploy consultado: `v1.4.3`, run 35405244798, sucesso. O CI geral também testa o workspace em Linux, macOS e Windows; não é necessário instalar um runner CI na VPS de produção.
- **Lacuna observada:** o pós-deploy consulta `/health` por loopback na VPS; o E2E MCP web usa relay temporário no runner. Falta uma verificação automatizada após publicação que use o domínio público/túnel e uma sessão descartável, confirme a versão em execução e teste ida/volta MCP sem tocar em projetos reais.
- **Resíduo de template:** comentários e `docker-compose.prod.yml` incluem referências/configuração de Postgres e migração, enquanto o relay atual guarda sessões em memória e não tem dependência de banco. Não remover infraestrutura de produção sem inventário de uso; registrar divergência e tornar geração/configuração específica para o serviço.

## Cozinha — detalhamento em andamento

- **Estado online confirmado, revisão 12:** bancada/pia e lava-louças alinhadas à face interna norte (y=12,5 cm), eliminando 17,5 cm de vão atrás. Coifa com costas na parede e mantendo 10 cm até a lateral da janela. Pendentes alinhados ao eixo da ilha e às banquetas.
- **Marcenaria acrescentada:** `f245`, módulo 70 × 60 × 87 cm com forno `f62` embutido, nicho livre 66,4 × 62 × 58,2 cm com base a 14 cm; `f257`, gaveteiro de 25 cm, três gavetas e puxadores perfil; `f286`, tampo de pedra clara 155 × 60 × 3 cm, altura 90 cm, cooktop `f61` embutido com recorte real de 56 × 46 cm. `f304`, frontão de 18 cm terminando a 108 cm, abaixo do peitoril a 110 cm; `f305`, arremate lateral de 7,5 cm.
- **Verificação:** dry-runs do alinhamento e complementos sem novos achados; `check_layout` local com regra nova e online retornou `{}`. Renderização em corte feita localmente e na aba real sem crash. Backup versionado atualizado. O conjunto continua em revisão de detalhamento, não pronto para fabricação.
- **P1 — Recorte não atravessa os anfitriões relacionados:** `embed` abre o tampo de pedra, mas `CabinetParams.cooktop` só aconselha profundidade e ainda gera o tampo integral de madeira (cabinet.rs). A pedra fica a 87–90 cm, o tampo de madeira a 85,2–87 cm, e o cooktop embutido começa a 84,6 cm: interferência sob a pedra. `check_layout` retorna vazio porque exclui componentes embutidos da colisão comum. **Pendente:** modelar recorte/travessas no armário e propagar recorte entre tampo e marcenaria associada; testar o volume oculto, não apenas o nicho e a imagem externa. Não aceitar genericamente essa interferência como “embutido”.
- **P2 — Nome decorativo ativa regra de circulação:** `box` chamada “Frontão de pedra — bancada norte” recebeu exigência de 85 cm de circulação como se fosse uma bancada operável. O frontão é vertical, tem 2 cm de espessura e base a 90 cm. Com o nome correto “Frontão de pedra — cozinha”, o falso positivo desapareceu. **Pendente:** papel semântico explícito para arremate/frontão; não deduzir função de qualquer ocorrência da palavra “bancada”. Manter o teste com nomes diferentes para garantir que nomenclatura não muda a geometria exigida.
- **P2 — Associação entre anfitriões ainda manual:** a IA calculou os módulos x=280–305 e 305–375, o recorte a 120 cm da extremidade do tampo e a posição do frontão sob o peitoril. O sistema deveria oferecer uma montagem relacionada (bancada, módulos, aparelhos, arremates) com dimensões derivadas, folgas e recortes sincronizados.

Atualização de publicação: o [Publish 35509917158](https://github.com/leandrodaf/3d-new-era-ai/actions/runs/35509917158), do commit `773aed8`, terminou com sucesso. As regras novas foram publicadas pelo GitHub; não houve alteração do relay nem necessidade de deploy VPS para esses fixes.

## Feedback de canto ocioso — pedido do usuário

- **Regra de trabalho:** informar a sobra, sem sair corrigindo. O usuário pediu para tratar o canto da cozinha somente na revisão final. Manter a posição da geladeira e o vazio enquanto os demais ambientes são desenvolvidos.
- **Implementado:** revisão de ergonomia identifica possíveis vazios em cantos ortogonais de cozinhas, cercados por duas paredes e móveis/eletrodomésticos. Mede o retângulo livre, informa posição, dimensões, área e IDs. É `dica`, sem `fix`, sem `probe` e sem penalidade de score. A interpretação pode ser aceita com justificativa (por exemplo, reserva de manutenção).
- **Heurística explícita:** lados entre 25 e 120 cm, área mínima de 0,16 m², peças próximas às duas paredes e abertura entre as peças de até 30 cm. Exclui cantos ocupados, acesso por porta/passagem, limites de ambientes abertos sem paredes e armários elevados. Não é uma regra normativa, otimização de área total ou prova de desperdício: a folga pode ter finalidade funcional.
- **Caso real confirmado no servidor recompilado:** `unused_corner:r14:f245+f64`, canto [454;12,5], 79 × 77,5 cm = 0,61225 m², entre módulo de cocção e geladeira. O relatório retornou peso 0 e nenhuma correção. A planta não foi modificada por essa análise.
- **Testes:** 25 testes de ergonomia passaram, incluindo vazio real, cantos rotacionados (90° e 33°), canto preenchido, passagem ampla, porta, ausência de paredes, móvel elevado, aceitação e ausência de correção/peso. Os 106 testes MCP passaram (um teste opcional ignorado), e Clippy do módulo passou. Limite atual: cantos de cozinha; outras sobras e volumes não retangulares permanecem fora do escopo desta heurística.

## Recorte no armário e continuidade dos interiores

- Commit `846b3ab`: `top_cutout=[x,y,w,d]` em cm cria vazio real no tampo de madeira e mantém uma única chapa usinada na lista de corte. Armário marcado `cooktop=true` sem recorte agora informa a pendência. 28 testes de marcenaria e 106 testes MCP passaram; Clippy aprovado. Publish e CI do commit passaram.
- Protótipo local do módulo `f245`: abertura de 62 × 52 cm a [4;4] da caixa deixa 1 cm além da projeção do aparelho em cada lado; lista de corte confirmou uma chapa MDF 18, 664 × 582 mm, recorte [22;40;620;520] mm. Isso libera o volume desenhado, sem certificar ventilação/instalação de um modelo comercial não escolhido. Aplicação na aba depende de carregar a versão nova do editor; a geração antiga não deve ser tratada como se entendesse o parâmetro.
- Banheiros online: louças e bancadas encostadas às faces corretas, gabinete da suíte ampliado para 100 cm com 69,5 cm até o vaso, piso social trocado por paginação regular. Espelhos com molduras e duas luminárias de 500 lm/3000 K acrescentados, revisão 14. Os espelhos são representação geométrica; o render simples não simula reflexão especular de espelho.
- **Liberdade de programa autorizada pelo usuário:** transformar quarto de serviço em lavanderia, com corredor à direita dando acessos independentes ao banheiro e à lavanderia, mantendo as janelas. Protótipo local: banho 6,19 m², lavanderia 5,99 m² e apoio 5,56 m², largura de circulação 132 cm; `check_layout` vazio. Ainda não aplicado online nesta etapa.
- **CI observado:** run 35510212447 falhou ao iniciar uma nova página depurável do Chrome no teste mobile (timeout de 30 s); os quatro tamanhos anteriores do editor passaram. O CI seguinte 35510573170 passou. Não confundir esse timeout do teste com novo panic do editor; registrar e investigar recorrência antes de atribuir causa definitiva.

### Atualização aplicada e confirmada online

- A aba carregou a versão com `top_cutout`: dry-run confirmou 15 componentes em vez do tampo fechado, seguido da atualização real de `f245`. A leitura dos componentes `f321`–`f324` confirmou vazio entre x=309–371 e y=16,5–68,5, com o cooktop dentro desse envelope. Backup versionado atualizado. A interferência específica sob o cooktop está corrigida na aba, não apenas no código local.
- Aplicada a redistribuição do bloco de serviço: parede `w312`, apoio `r313`, porta do banho `f314`, lavadora `f315`, secadora `f316` e tanque `f317`; cama de serviço removida e armário reaproveitado como rouparia. Porta `f42` dá acesso ao apoio, `f43` passa a dar acesso independente à lavanderia; banheiro mantém a janela oeste e lavanderia mantém a janela sul. Os dois quartos principais permanecem.
- `check_layout` retornou `{}` e `render_plan` 1100 × 780 funcionou na aba após as alterações. Checkpoint “Lavanderia independente, banheiros e recorte do cooktop”. Bancada/armários complementares da lavanderia, detalhes decorativos, cotas e revisão visual final continuam pendentes.
- O canto da cozinha assinalado pelo usuário **não foi preenchido nem reposicionado**. A correção dele permanece reservada à revisão final. Commit `f13f1aa` enviado com o feedback; Publish/CI em andamento na última consulta.

## Controle de IA na barra de ferramentas

- Pedido do usuário: mover o controle de IA para um grupo próprio imediatamente após Frame, exibindo somente o robô. Removidas as versões duplicadas no menu superior e na barra de status; tooltip e nome acessível continuam informando a conexão e as chamadas.
- Preservadas as cores de desconectado, aguardando, conectado e atividade recente. A atualização periódica acompanha a passagem de atividade para repouso mesmo sem movimento do mouse. Histórico de agente não deve indicar conexão ativa quando o servidor está desligado.
- Automação de regressão: testes de interface verificam a posição após Frame, abertura do painel, identificação do agente/chamadas e presença na barra rolável em telas estreitas. Suíte do aplicativo: 66 testes passaram, 16 testes opcionais ignorados; Clippy sem avisos.
- Validação web: build WASM concluído; E2E do editor em Chrome/WebGPU passou (desenho, imagem de fundo, autosave/reload e viewer). Screenshot conferido visualmente: robô isolado após Frame. E2E MCP passou com conexão, 42 ferramentas, alterações na planta, PNGs 2D/3D, reload, desligamento e reconexão no mesmo endereço.

## Renderização sem bloquear a interface — imagens e vídeo

- **Causa reproduzida no código:** `PhotoWindow::start` executava o path tracer sincronamente no WASM antes de registrar o job; nem o spinner conseguia aparecer. `render_plan`/`render_3d` chamados por MCP também calculavam na thread da página. O botão web de vídeo não tinha implementação de exportação.
- **Correção:** fotos, imagens MCP e vídeo web usam um worker descartável com uma instância WASM independente. O editor permanece livre para desenhar a interface e responder a comandos. O worker termina ao concluir, falhar, cancelar ou fechar a janela de foto/vídeo. O vídeo web agora baixa um AVI; o MCP `video`, que exige caminho de arquivo, permanece exclusivo do aplicativo. `render_photo` passa a ser oferecido no MCP web.
- **Progresso e previsão:** barra por linhas da foto ou quadros do vídeo, etapa de preparação/finalização e tempo restante estimado a partir da taxa já observada. Preparação sem quantidade conhecida não inventa porcentagem nem prazo. A previsão varia com a cena e não é garantia de horário; PNGs de planta/3D mostram a etapa em andamento, sem estimativa por linhas.
- **Orçamento:** uma renderização pesada por vez na aba (foto, vídeo e MCP compartilham o bloqueio). Worker usa um único thread de cálculo; foto nativa usa no máximo metade dos processadores disponíveis, limitada a quatro, inclusive quando há override por variável de ambiente. Web limita imagens a 1.228.800 pixels, snapshot JSON a 16 MiB e arquivos montados a 32 MiB. Vídeo limita 900 quadros web / 1.800 nativos e 32 MiB de JPEGs antes de montar o AVI; interrupção ocorre ao exceder o orçamento. Limites reduzem consumo, não representam uma reserva absoluta de RAM do navegador nem controlam outras abas/aplicativos.
- **Cancelamento:** no navegador encerra o worker; nativo verifica a solicitação entre linhas/quadros. Fechar a janela também cancela. Falha e cancelamento liberam a vaga para nova renderização. MCP encerra após 150 s, antes do timeout de 180 s do relay, evitando manter cálculo órfão após expirar a chamada.
- **Assets e cache:** modelos/texturas montados são copiados com orçamento para o worker; snapshot preserva os arquivos após desmontagem. Glue JS e WASM do worker usam o mesmo identificador de build da página, evitando misturar releases pelo cache.
- **Verificação inicial:** 68 testes do aplicativo e 29 do renderizador passaram (16 visuais opcionais ignorados no aplicativo). Cobertura inclui cancelamento, liberação de orçamento, limites de resolução/fps/duração e parada de vídeo antes de gerar quadros. Chrome/WebGPU gerou PNGs de planta, 3D e foto em workers, manteve heartbeat da página e gerou AVI com progresso; resolução excessiva foi recusada.
- **Pendências de automação identificadas:** dimensionar orçamento por memória disponível do dispositivo; enviar somente assets usados pela cena (snapshot atual inclui arquivos montados); tornar preparação/PNG 2D/3D mensuráveis para ETA; migrar outras operações de arquivo ainda síncronas no WASM. Não confundir essas pendências com o processamento de foto/vídeo, agora isolado.
- **Verificação complementar:** 106 testes MCP passaram (um opcional ignorado), além de dois testes VFS. Clippy nativo e WASM sem avisos. E2E confirmou exclusão de render simultâneo, `get_home` respondendo durante foto pesada, falha simulada de worker com recuperação e botão Cancelar encerrando o worker/liberando nova renderização.
- **Fluxo de vídeo no navegador:** havia ainda um bloqueio de disponibilidade no menu “Criar vídeo”; removido ao ligar a exportação web. Teste manual pelo menu adicionou dois pontos, gerou e baixou AVI de 98.412 bytes. `ffprobe` confirmou MJPEG, 320 × 240, cinco quadros. Esse percurso foi acrescentado ao E2E para não depender apenas de chamar o worker diretamente. Pequeno defeito visual preexistente observado: caminho vazio pode mostrar duração `-0.0 s`; registrar para revisão de apresentação, sem impacto no render.
- **Validação final:** E2E WebGPU passou com imagens, responsividade, falha/recuperação e cancelamento. E2E WebGL passou também pelo menu “Criar vídeo” até baixar um AVI válido, além de reload/reconexão MCP. O teste fixa viewport antes de clicar no menu, pois o popup muda de posição quando não cabe na altura da janela; isso evita confundir erro de coordenada do teste com regressão do produto. Formatação e checagem de diff aprovadas.

## Lavanderia — marcenaria aplicada e conferida

- Aplicado na aba: bancada `f333` de 150 × 75 cm, topo a 91 cm; armário `f335` de 150 × 35 × 90 cm elevado a 155 cm, três portas e puxadores em perfil. Apoios `f355`–`f357`, prateleira `f358`, proteção lavável `f359`, perfil LED `f360` (900 lm, 3500 K) e vaso `f361`. Tanque recebe acabamento coerente com a marcenaria.
- **Cálculos manuais:** máquinas afastadas 5 cm da parede; três apoios de 18 mm ficam fora dos volumes dos aparelhos, incluindo o vão de 10 cm entre eles. Bancada tem face inferior a 88 cm, acima dos aparelhos de 85 cm. O gerador de bancada oferecia mãos-francesas padrão; optei por apoios explicitamente desenhados para não colocar suportes no espaço das máquinas. Folgas representam o estudo de interiores e ainda dependem do modelo comercial escolhido para instalação/manutenção.
- **Verificação:** `check_layout` retornou `{}`; renderizações de planta e da câmera “Lavanderia — bancada e armários” funcionaram na aba. Leitura 3D confirmou bancada, laterais/divisória e máquinas no mesmo conjunto. Checkpoint criado. Cópia nativa atualizada com os mesmos IDs; salvamento do backup versionado confirmado, 162 peças, 14 paredes e 11 ambientes.
- **Automação desejada:** bancada sobre eletrodomésticos deveria sugerir apoios fora dos envelopes de serviço, com folgas explícitas. Em planta, a projeção opaca do armário superior esconde a leitura das máquinas; convém oferecer representação tracejada/transparente apenas no desenho técnico, preservando o material 3D. A luminária nova fez o contador de pontos sem circuito passar de 60 para 61: circuitos e instalações continuam pendentes, sem fingir que `check_layout={}` os certifica.
- O canto ocioso da cozinha permanece intacto para a revisão final. Demais interiores, cotas, anotações e revisão global continuam em andamento.

## CI Windows — teste de snapshot VFS

- Run `35513282420`, job Windows `106084844039`: 155 testes core passaram, um foi ignorado e o novo teste de snapshot falhou ao procurar um caminho com `ends_with("render-snapshot-test/a.bin")`. O arquivo estava presente com separadores nativos do Windows; a busca textual retornou `None`.
- Correção: comparar com `Path::new(p) == dir.join("a.bin")`, mantendo a verificação de orçamento e de posse dos bytes após desmontagem. A alteração é na asserção do teste. Não atribuir essa falha ao runtime do navegador. A validação Windows ocorrerá novamente no GitHub.

## Publicação — fixture de idioma no E2E

- Publish `35513282527` passou geração de PNGs, responsividade, exclusão de renders, falha/recuperação, cancelamento e AVI no worker; falhou ao procurar o AVI baixado pelo fluxo do menu. O log não registrou panic do editor.
- O teste de cliques tinha coordenadas de menus em português e dependia do idioma padrão do navegador. Agora a fixture fixa `navigator.language/languages` em pt-BR antes de carregar a página e verifica que isso foi aplicado; a altura do viewport já é fixa para o menu não mudar de lado. Não muda o idioma do produto em uso normal. E2E local completo repetido com sucesso, incluindo o download pelo menu e reconexão.

## Revisão técnica ainda pendente

- A revisão de ergonomia da planta atual continua com score 0 principalmente por instalações não desenvolvidas: tomadas, circuitos, água e esgoto. Não usar o score como certificação nem inventar infraestrutura apenas para subir a nota.
- Observação para melhorar o relatório: uma mensagem agrupada de tomadas reuniu Jantar/Estar/Galeria e banheiros sob a justificativa “um junto ao lavatório”. O agrupamento deve preservar a justificativa de cada tipo de ambiente antes de orientar alterações. Recomendações de TV/rede aparecem inclusive em áreas de serviço/banhos; verificar a interpretação e contexto, sem instalar pontos automaticamente só para apagar dicas.
- A suíte recebe dica de ausência de guarda-roupa apesar do ambiente Closet adjacente. Revisar a associação entre ambientes e capacidade de armazenamento na etapa global, com evidência de acesso e móveis, em vez de aceitar/silenciar automaticamente.
